driver:
  name: docker
platforms:
  - name: molecule-gameserver
    groups:
      - gameservers
    image: ubuntu:21.04
    pre_build_image: false
provisioner:
  name: ansible
  playbooks:
    converge: ../../gameservers.yml
  # ansible_args:
scenario:
  test_sequence:
    - destroy
    - syntax
    - create
    - converge
    - idempotence
    # - verify
    - destroy

